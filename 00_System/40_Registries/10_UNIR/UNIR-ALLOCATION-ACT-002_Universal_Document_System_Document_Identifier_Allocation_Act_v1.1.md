# Universal Document System Concrete Document Identifier Allocation Act

**Allocation Act ID:** UNIR-ALLOCATION-ACT-002  
**Document Type:** Concrete Document Identifier Allocation Act  
**Version:** 1.1  
**Supersedes:** `UNIR-ALLOCATION-ACT-002 v1.0`  
**Canonicality:** CANONICAL  
**Lock Status:** LOCKED  
**Status:** LOCKED — CANONICALIZED  
**Publication Status:** PUBLISHED  
**Authority:** UNIR Registry Authority  
**Target:** Universal Document System  
**Target Document ID:** `UDS-CORE-001`  
**Allocated Identifier:** `DIUA-DIC-000004`  
**Identifier Class:** DIC  
**Namespace:** DIUA  
**Allocation State:** ALLOCATED — ACTIVE  
**Grammar:** `DIUA-DIC-<6DigitSequence>`  
**Allocation Sequence:** `000004`

## Revision Purpose

This successor revision updates the documentary traceability of `UNIR-ALLOCATION-ACT-002` while preserving the source boundary of the historical Allocation Act.

The current source establishes the allocation identity, target, concrete Document Identifier, allocation state, and relevant current UDS identity. It does not provide the original full substantive historical body of the Allocation Act. No unsupported historical wording is reconstructed.

## 1. Allocation Determination

The current UNIR Registry State explicitly establishes the following concrete Document Identifier allocation:

```text
DIUA-DIC-000004
        ↓
Universal Document System
```

The registry records:

```text
Target Document:
Universal Document System

Allocation State:
ALLOCATED — ACTIVE

Allocation Act:
UNIR-ALLOCATION-ACT-002
```

This standalone artifact materializes the established Allocation Act identity and source-supported allocation facts without inventing substantive historical act text that is not present in the current source.

## 2. Allocation Scope

The allocated identifier applies specifically to:

**Universal Document System**

The applicable current UDS source identifies the document identity as `UDS-CORE-001` and provides the current governed UDS baseline as v1.5. The allocation source itself does not state a historical target version for the allocation and therefore no historical version is inferred here.

The allocation concerns the concrete Document Identifier:

```text
DIUA-DIC-000004
```

No new Namespace, Identifier Class, identifier grammar, or UNIR Registry Object is created by this materialization.

## 3. Allocation State

```text
Allocation State: ALLOCATED — ACTIVE
```

The current UNIR Registry State records `DIUA-DIC-000004` as actively allocated to Universal Document System.

The current source establishes the allocation state and Allocation Act reference, but does not provide the original full substantive text of `UNIR-ALLOCATION-ACT-002`. Accordingly, this artifact is marked `RECONSTRUCTED — SOURCE-BOUND` rather than `VERIFIED — ACTIVE EVIDENCE`.

## 4. Allocation Boundary

```text
Concrete Document Identifier
    ≠
Allocation Act
    ≠
Registration Record
    ≠
UNIR Registry Object
    ≠
Current Registry State
```

This artifact represents the Allocation Act identity and the source-supported allocation facts. It does not collapse the Allocation Act into the Registration Record or Current Registry State.

## 5. Registry Traceability

The current validated UNIR baseline establishes the following traceability relationship:

```text
UNIR-ALLOCATION-ACT-002
        ↓
DIUA-DIC-000004
        ↓
UDS-CORE-001
        ↓
Universal Document System
        ↓
UNIR-REGISTRY-001 v1.5
```

The current UDS source independently identifies `UDS-CORE-001` as the Universal Document System document identity.

No additional historical publication, registration, or allocation-decision wording is inferred here.

## Current-State Traceability Bridge

The current validated representations establish:

```text
UNIR-ALLOCATION-ACT-002
        ↓
DIUA-DIC-000004
        ↓
UDS-CORE-001
        ↓
Universal Document System
        ↓
UNIR-REGISTRY-001 v1.5
```

This bridge records current traceability only. It does not reconstruct, replace, or retroactively rewrite the unavailable original substantive body of the historical Allocation Act.

### Canonicalization Declaration

The authorized user declares this successor revision **CANONICALIZED and LOCKED**. This lifecycle declaration applies to the present documentary artifact and does not retroactively reconstruct unavailable historical source text.

## 6. Canonical Closure

Canonicalization State: CANONICALIZED
Lock State: LOCKED
Publication State: PUBLISHED

```text
Allocation Act:       UNIR-ALLOCATION-ACT-002
Allocated ID:         DIUA-DIC-000004
Target:               Universal Document System
Target Document ID:   UDS-CORE-001
Allocation State:     ALLOCATED — ACTIVE
Evidence State:       RECONSTRUCTED — SOURCE-BOUND
```

### Evidence Relationship

```text
UNIR-ALLOCATION-ACT-002
        ↓
DIUA-DIC-000004
        ↓
UDS-CORE-001
        ↓
Universal Document System
```

The Allocation Act remains a distinct artifact and does not become the Registration Record, Registry Object, or Current Registry State.

### Historical Evidence Boundary

The original full substantive body of `UNIR-ALLOCATION-ACT-002` is not present in the available source material. This successor revision therefore preserves the historical evidence boundary.

No unsupported historical wording, publication state, lock state, or canonicalization state is inferred.

### Source-Boundary Note

The current uploaded UNIR Registry source establishes the existence/reference of `UNIR-ALLOCATION-ACT-002`, its target, concrete identifier, namespace, identifier class, allocation sequence, and active allocation state.

The current uploaded UDS source establishes `UDS-CORE-001` as the document identity of Universal Document System and provides the current governed UDS baseline as v1.5.

Neither source provides the original full textual body of the historical Allocation Act. No unsupported historical wording has been supplied in this reconstruction.
