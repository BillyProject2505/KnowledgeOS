# Universal Architecture Concrete Document Identifier Allocation Act

**Allocation Act ID:** UNIR-ALLOCATION-ACT-001  
**Document Type:** Concrete Document Identifier Allocation Act  
**Version:** 1.1  
**Supersedes:** `UNIR-ALLOCATION-ACT-001 v1.0`  
**Canonicality:** CANONICAL  
**Lock Status:** LOCKED  
**Status:** LOCKED — CANONICALIZED  
**Authority:** UNIR Registry Authority  
**Target:** Universal Architecture  
**Target Document ID:** `UA-CORE-001`  
**Allocated Identifier:** `DIUA-DIC-000002`  
**Identifier Class:** DIC  
**Namespace:** DIUA  
**Allocation State:** ALLOCATED — ACTIVE  
**Grammar:** `DIUA-DIC-<6DigitSequence>`  
**Allocation Sequence:** `000002`

## 1. Allocation Determination

The current UNIR Registry State explicitly establishes the following concrete Document Identifier allocation:

```text
DIUA-DIC-000002
        ↓
Universal Architecture
```

The registry identifies the target document as:

```text
UA-CORE-001 v0.2
Universal Architecture
```

and records the allocation state as:

```text
ALLOCATED — ACTIVE
```

The same registry source explicitly identifies:

```text
Allocation Act Reference:
UNIR-ALLOCATION-ACT-001
```

This standalone artifact materializes that established Allocation Act identity without inventing substantive historical act text that is not present in the current uploaded source.

## 2. Allocation Scope

The allocated identifier applies specifically to:

**Universal Architecture**

**Target Document ID:** `UA-CORE-001`

The allocation concerns the concrete Document Identifier:

```text
DIUA-DIC-000002
```

No new Namespace, Identifier Class, identifier grammar, or UNIR Registry Object is created by this materialization.

## 3. Allocation State

```text
Allocation State: ALLOCATED — ACTIVE
```

The current UNIR Registry State records `DIUA-DIC-000002` as actively allocated to `UA-CORE-001 v0.2 — Universal Architecture`.

The current source establishes the allocation state, but does not provide the original full substantive text of `UNIR-ALLOCATION-ACT-001`. Accordingly, this artifact preserves the historical evidence boundary and does not infer unavailable historical act text.

## 4. Allocation Boundary

```text
Concrete Document Identifier
    ≠
Allocation Act
    ≠
Registration Record
    ≠
UNIR Registry Object
    ≠
Current Registry State
```

This artifact represents the Allocation Act identity and the source-supported allocation facts. It does not collapse the Allocation Act into the Registration Record or Current Registry State.

## 5. Registry Traceability

The current uploaded `UNIR-REGISTRY-001 v1.5` establishes the following traceability relationship:

```text
UNIR-ALLOCATION-ACT-001
        ↓
DIUA-DIC-000002
        ↓
UA-CORE-001 v0.2
        ↓
Universal Architecture
        ↓
UNIR-REGISTRY-001 v1.5
```

The source explicitly describes `UNIR-ALLOCATION-ACT-001` as the Allocation Act reference declared by Universal Architecture for `DIUA-DIC-000002`.

No additional historical publication or registration details are inferred here.

## Current-State Traceability Bridge

The current validated UNIR baseline establishes the following current-state relationship:

```text
UNIR-ALLOCATION-ACT-001
        ↓
DIUA-DIC-000002
        ↓
UA-CORE-001
        ↓
Universal Architecture
        ↓
UNIR-REGISTRY-001 v1.5
```

This bridge records current traceability only. It does not reconstruct or replace the unavailable original substantive body of the historical Allocation Act.

### Canonicalization Declaration

The authorized user declares this successor revision **CANONICALIZED and LOCKED**. This lifecycle declaration applies to the present documentary artifact and does not retroactively reconstruct unavailable historical source text.

## 6. Canonical Closure

Canonicalization State: CANONICALIZED
Lock State: LOCKED
Publication State: NOT PUBLISHED

```text
Allocation Act:       UNIR-ALLOCATION-ACT-001
Allocated ID:         DIUA-DIC-000002
Target:               Universal Architecture
Target Document ID:   UA-CORE-001
Allocation State:     ALLOCATED — ACTIVE
Evidence State:       RECONSTRUCTED — SOURCE-BOUND
```

### Evidence Relationship

```text
UNIR-ALLOCATION-ACT-001
        ↓
DIUA-DIC-000002
        ↓
UA-CORE-001
        ↓
Universal Architecture
```

The Allocation Act remains a distinct artifact and does not become the Registration Record, Registry Object, or Current Registry State.

### Historical Evidence Boundary

The original full substantive body of `UNIR-ALLOCATION-ACT-001` is not present in the available source material. This canonicalized successor revision preserves that historical evidence boundary. No unsupported historical wording, publication state, lock state, or canonicalization state is inferred.

### Source-Boundary Note

The current uploaded source establishes the existence/reference of `UNIR-ALLOCATION-ACT-001`, its target, concrete identifier, namespace, identifier class, allocation sequence, and active allocation state. It does not provide the original full textual body of the historical Allocation Act. No unsupported historical wording has been supplied in this reconstruction.
