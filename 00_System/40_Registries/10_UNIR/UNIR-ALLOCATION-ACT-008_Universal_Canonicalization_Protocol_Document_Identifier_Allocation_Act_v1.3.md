---
allocation_act_id: UNIR-ALLOCATION-ACT-008
document_type: Concrete Document Identifier Allocation Act
version: "1.3"
status: VERIFIED — ACTIVE EVIDENCE
supersedes: UNIR-ALLOCATION-ACT-008 v1.2
authority: UNIR Registry Authority
target: Universal Canonicalization Protocol
allocated_identifier: DIUA-DIC-000008
identifier_class: DIC
namespace: DIUA
allocation_state: ALLOCATED — ACTIVE
grammar: DIUA-DIC-<6DigitSequence>
allocation_sequence: "000008"
---

# Universal Canonicalization Protocol Concrete Document Identifier Allocation Act

**Allocation Act ID:** `UNIR-ALLOCATION-ACT-008`  
**Document Type:** Concrete Document Identifier Allocation Act  
**Version:** 1.3  
**Supersedes:** `UNIR-ALLOCATION-ACT-008 v1.2`  
**Canonicality:** CANONICAL  
**Lock Status:** LOCKED  
**Status:** VERIFIED — ACTIVE EVIDENCE  
**Authority:** UNIR Registry Authority  
**Target:** Universal Canonicalization Protocol (UCP)  
**Allocated Identifier:** `DIUA-DIC-000008`  
**Identifier Class:** DIC  
**Namespace:** DIUA  
**Allocation State:** ALLOCATED — ACTIVE  
**Grammar:** `DIUA-DIC-<6DigitSequence>`  
**Allocation Sequence:** `000008`

## Revision Purpose

This successor revision materializes the explicit allocation authorization for `DIUA-DIC-000008` while preserving the document artifact's canonicalized and locked state. The identifier is no longer a candidate and is now `ALLOCATED — ACTIVE`.

The authorization applies to the allocation decision represented by this Allocation Act. Downstream Registration Record and Current Registry State materialization shall proceed through their controlled successor revisions.

No retroactive alteration is made to the previously published proposal revision `v1.2`.

## 1. Allocation Determination

This Allocation Act records the authorized concrete Document Identifier allocation:

```text
DIUA-DIC-000008
        ↓
Universal Canonicalization Protocol
```

The allocation was derived as the next available sequence from the validated current allocation state. `DIUA-DIC-000007` was the latest active allocation in the validated baseline, and `DIUA-DIC-000008` is now explicitly authorized as an active allocation.

## 2. Allocation Scope

The allocated identifier applies specifically to:

**Universal Canonicalization Protocol**

**Short Name:** UCP  
**Document Type:** Universal Canonicalization Protocol

The allocation does not create a UNIR Registry Object, does not redefine the DIC Identifier Class, does not create a new DIUA namespace, and does not alter UNIS normative naming or identification semantics.

## 3. Allocation State

```text
Allocation State: ALLOCATED — ACTIVE
```

The identifier has completed the applicable allocation authorization gate and is now `ALLOCATED — ACTIVE`.

This act constitutes active allocation evidence for `DIUA-DIC-000008`.

## 4. Allocation Boundary

```text
Concrete Document Identifier
    ≠
Allocation Act
    ≠
Registration Record
    ≠
UNIR Registry Object
    ≠
Current Registry State
```

This artifact represents the authorized Allocation Act and active allocation evidence. It does not collapse the Allocation Act into the Registration Record or Current Registry State.

## 5. Source Basis and Traceability

The allocation determination is based on the validated UNIR source baseline:

- `UNIR-REGISTRY-001 v1.5` — current active concrete Document Identifier allocation count: 7, with `DIUA-DIC-000007` as the latest active allocation at the time of authorization.
- `UNIR-REGISTRATION-RECORD-001 v1.5` — corroborating representation of `DIUA-DIC-000005`, `DIUA-DIC-000006`, and `DIUA-DIC-000007` allocation evidence.
- `Universal_Canonicalization_Protocol_v0.1` — identifies the target document as Universal Canonicalization Protocol, Short Name `UCP`, and states that its Document ID is to be established through applicable identity allocation.

## Current-State Traceability Bridge

The current validated UNIR baseline establishes the following allocation sequence relationship:

```text
UNIR-REGISTRY-001 v1.5
        ↓
7 active DIC allocations
        ↓
DIUA-DIC-000007
        ↓
DIUA-DIC-000008
        ↓
Universal Canonicalization Protocol (UCP)
```

This bridge represents the authorized active allocation determination. It establishes active allocation evidence at the Allocation Act layer; downstream registration and current registry-state materialization remain separate controlled steps.

## Allocation Authorization

```text
Authorization State:        AUTHORIZED
Authorized Identifier:      DIUA-DIC-000008
Target:                     Universal Canonicalization Protocol
Authorization Basis:       Current validated UNIR allocation sequence
Previous Active Allocation: DIUA-DIC-000007
Authorization Decision:    AUTHORIZE DIUA-DIC-000008
Resulting Allocation State: ALLOCATED — ACTIVE
Evidence State:             VERIFIED — ACTIVE EVIDENCE
```

This authorization is explicit and applies to the Allocation Act layer. It does not retroactively alter the previously published proposal revision `v1.2`.

## 6. Canonical Closure

```text
Allocation Act:          UNIR-ALLOCATION-ACT-008
Allocated ID:            DIUA-DIC-000008
Target:                  Universal Canonicalization Protocol
Allocation State:        ALLOCATED — ACTIVE
Evidence State:          VERIFIED — ACTIVE EVIDENCE
Authorization State:     AUTHORIZED
Canonicalization State:  CANONICALIZED
Lock State:              LOCKED
Publication State:       NOT PUBLISHED
```

### Canonicalization Declaration

The authorized user declares this successor revision **CANONICALIZED and LOCKED** and authorizes `DIUA-DIC-000008` as an active allocation. This lifecycle declaration applies to the present documentary artifact; downstream registry-state materialization remains a separate controlled step.

### Evidence Relationship

```text
UNIR-ALLOCATION-ACT-008
        ↓
DIUA-DIC-000008
        ↓
Universal Canonicalization Protocol
```

This relationship is an authorized active allocation determination. It does not by itself constitute downstream Registration Record or Current Registry State materialization.

## Decision Boundary

`DIUA-DIC-000008` is authorized as `ALLOCATED — ACTIVE` at the Allocation Act layer. It shall be materialized into the applicable UNIR Registration Record and Current Registry State only through their controlled successor revisions. The active allocation authorization shall not be inferred to mean that those downstream representations have already been updated.
